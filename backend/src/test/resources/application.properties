spring.datasource.url=jdbc:mysql://localhost:3306/ironbank_test
spring.datasource.username=ironhacker
spring.datasource.password=1r0nh4ck3r
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update

server.error.include-message=always
server.error.include-binding-errors=always

# Swagger configuration
spring.mvc.pathmatch.matching-strategy = ANT_PATH_MATCHER

# Keycloak configuration
keycloak.realm = tutorial
keycloak.auth-server-url = http://localhost:8181
keycloak.ssl-required = external
keycloak.resource = tutorial-backend
keycloak.use-resource-role-mappings = true
keycloak.bearer-only = true
custom.keycloak.realm-master = master
custom.keycloak.username = sjacaspuig
custom.keycloak.password = sjacaspuig28
custom.keycloak.client-id = admin-cli

# Iban constants
iban.countrycode=ES
iban.checksum=28
iban.bankcode=1043
iban.branchcode=0001
iban.bankchecksum=10

# Is test
custom.is.test=true
