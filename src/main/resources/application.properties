spring.datasource.url=jdbc:mysql://localhost:3306/ironbank
spring.datasource.username=ironhacker
spring.datasource.password=1r0nh4ck3r
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update

server.error.include-message=always
server.error.include-binding-errors=always

# Swagger configuration
spring.mvc.pathmatch.matching-strategy = ANT_PATH_MATCHER

# Keycloak configuration
keycloak.realm = ironbank
keycloak.auth-server-url = http://localhost:8181/
keycloak.ssl-required = external
keycloak.resource = ironbank-backend
keycloak.use-resource-role-mappings = true
keycloak.bearer-only = true
custom.keycloak.realm.master = master
custom.keycloak.master.username = sjacaspuig
custom.keycloak.master.password = sjacaspuig28
custom.keycloak.master.resource = admin-cli

#Keycloak starter configuration
#.kc.bat start-dev --http-port 8181